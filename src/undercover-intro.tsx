import {useEffect,useRef,useState} from "react";
export function UndercoverIntro({onClose}:{onClose:()=>void}){
 const [hide,setHide]=useState(false),dialog=useRef<HTMLDivElement>(null);
 const close=()=>{try{localStorage.setItem("blendin_undercover_intro_hidden",hide?"1":"0")}catch{}onClose()};
 useEffect(()=>{const previous=document.activeElement as HTMLElement|null;dialog.current?.focus();const overflow=document.body.style.overflow;document.body.style.overflow="hidden";return()=>{document.body.style.overflow=overflow;previous?.focus()}},[]);
 return <div className="hint-modal-backdrop"><div ref={dialog} tabIndex={-1} className="hint-modal undercover-intro" role="dialog" aria-modal="true" aria-labelledby="undercover-title" onKeyDown={e=>{if(e.key==="Escape"){e.preventDefault();close()}if(e.key==="Tab"){const items=dialog.current?.querySelectorAll<HTMLElement>('input,button');if(!items?.length)return;const first=items[0],last=items[items.length-1];if(e.shiftKey&&(document.activeElement===first||document.activeElement===dialog.current)){e.preventDefault();last.focus()}else if(!e.shiftKey&&(document.activeElement===last||document.activeElement===dialog.current)){e.preventDefault();first.focus()}}}}><h2 id="undercover-title">Meet Undercover</h2><p>Everyone gets a word. One player gets a different but related word, and nobody is told their role.</p><p>For example, most players might see Beach while one sees Desert.</p><ol><li>Hold your card to read your word privately.</li><li>Take turns giving clues without saying your word.</li><li>Discuss who might have the different word. Vote if voting is enabled.</li></ol><p>If Undercover is among the players with the most votes, the other players win. Otherwise, Undercover wins. Tied players are all accused. If Escape is enabled, a caught Undercover can first try to guess the majority’s word.</p><p>English categories only, with one Undercover and no Jester. Your voting, Escape, scoring, timer and card settings apply. With voting off, reveal both words and the hidden player without declaring a winner.</p><label><input type="checkbox" checked={hide} onChange={e=>setHide(e.target.checked)}/> Don’t show again</label><button className="primary wide" onClick={close}>Close</button></div></div>
}
